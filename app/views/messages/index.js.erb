jQuery("#messages-list").append("<%= escape_javascript(render partial: 'messages') %>");
jQuery("#show-more").attr('href', "<%= root_path(next_page: @next_page) %>");
jQuery('.loader').hide();
ReactRailsUJS.mountComponents();